<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:background="#303040" >

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:gravity="center_horizontal"
        android:orientation="horizontal" >
        <ImageView
            android:id="@+id/splash_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginLeft="10dp"
            android:gravity="center"
            android:src="@drawable/ic_launcher" />
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:textColor="#AFDEFE"
            android:textSize="28sp"
            android:typeface="serif"
            android:textStyle="italic"
            android:text="@string/app_name" />
    </LinearLayout>

    <com.facebook.widget.LoginButton
        android:id="@+id/facebook_login_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="130dp"
        android:layout_marginBottom="30dp" />

    <Button
        android:id="@+id/menu_button"
        android:text="Go to the Menu"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal|clip_vertical"
        android:layout_marginTop="30dp"
        android:padding="10dp"
        android:layout_marginBottom="30dp"
        style="@style/com_facebook_loginview_default_style"
        android:onClick="startApplicationActivity"/>


</LinearLayout>


    <!--
    <com.applications.frodo.widgets.LoginButton
        android:id="@id/login_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="70dp"
        android:layout_marginBottom="15dp"
        android:background="@color/login_button_bgcolor"
        android:textColor="@color/login_button_textcolor"
        android:paddingLeft="@dimen/com_loginbutton_padding_left"
        android:paddingRight="@dimen/com_loginbutton_padding_right"
        android:paddingBottom="@dimen/com_loginbutton_padding_bottom"
        android:paddingTop="@dimen/com_loginbutton_padding_top"
        android:textSize="@dimen/com_loginbutton_text_size"
        android:textStyle="bold"
        android:text="@string/login_button_text"
        />
    -->